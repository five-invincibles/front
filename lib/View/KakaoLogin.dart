import 'package:flutter/material.dart';

class KakaoLogin extends StatelessWidget {
  const KakaoLogin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("카카오 로그인 페이지"),
      ),
    );
  }
}
